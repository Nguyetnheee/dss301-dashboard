# Local development: http://127.0.0.1:8000/predict
# Vercel deployment: /api/predict
MODEL_API_URL=http://127.0.0.1:8000/predict

# Do not expose this value to browser code. This static Vite deployment does not
# send API keys; use a server-side proxy if the external service requires one.
MODEL_API_KEY=
